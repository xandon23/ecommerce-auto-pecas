<?php include BASE_PATH . '/views/partials/header.phtml'; ?>

<div class="container my-5">

    <div class="d-flex align-items-center gap-2 mb-4">
        <i class="bi bi-bag-check fs-2 text-danger"></i>
        <h2 class="fw-bold mb-0 text-dark">Meus Pedidos</h2>
    </div>

    <?php if (empty($pedidos)): ?>
        <div class="alert alert-light border shadow-sm p-5 text-center rounded-3">
            <i class="bi bi-emoji-smile display-1 text-muted mb-3 d-block"></i>
            <h4 class="fw-bold">Você ainda não fez nenhuma compra.</h4>
            <p class="text-muted mb-4">Que tal dar uma olhada nas nossas ofertas?</p>
            <a href="<?= BASE_URL ?>/home" class="btn btn-brand px-4">
                Ir para a loja
            </a>
        </div>
    <?php else: ?>
        <div class="card shadow-sm border-0">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0 align-middle">
                        <thead class="table-light">
                            <tr>
                                <th># Pedido</th>
                                <th>Data</th>
                                <th>Status</th>
                                <th>Total</th>
                                <th class="text-end pe-4">Ação</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($pedidos as $p): ?>
                                <tr>
                                    <td class="fw-bold text-dark">#<?= $p->id_pedido ?></td>
                                    <td class="text-muted"><?= date('d/m/Y H:i', strtotime($p->data_pedido)) ?></td>
                                    <td>
                                        <?php
                                        $cor = 'bg-secondary';
                                        $texto = $p->status;

                                        if ($p->status == 'Confirmado') {
                                            $cor = 'bg-success';
                                        }
                                        if ($p->status == 'Pendente') {
                                            $cor = 'bg-warning text-dark';
                                            $texto = 'Aguardando';
                                        }
                                        if ($p->status == 'Cancelado') {
                                            $cor = 'bg-danger';
                                        }
                                        ?>
                                        <span class="badge <?= $cor ?> rounded-pill"><?= $texto ?></span>
                                    </td>
                                    <td class="fw-bold text-dark">R$ <?= number_format($p->valor_total, 2, ',', '.') ?></td>
                                    <td class="text-end pe-3">
                                        <a href="<?= BASE_URL ?>/pedido/detalhes/<?= $p->id_pedido ?>" class="btn btn-sm btn-outline-primary">
                                            Ver Detalhes
                                        </a>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    <?php endif; ?>
</div>

<?php include BASE_PATH . '/views/partials/footer.phtml'; ?>