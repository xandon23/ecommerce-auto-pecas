<?php include BASE_PATH . '/views/partials/header.phtml'; ?>

<div class="container my-5">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-dark">Detalhes do Pedido <span class="text-secondary">#<?= $id_pedido ?></span></h2>
        <a href="<?= BASE_URL ?>/pedido/listar" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Voltar
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-header bg-white py-3 fw-bold">
            <i class="bi bi-box-seam"></i> Itens comprados
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th style="width: 80px;">Item</th>
                            <th>Produto</th>
                            <th>Preço Unit.</th>
                            <th>Qtd</th>
                            <th class="text-end pe-4">Subtotal</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php
                        $total = 0;
                        foreach ($itens as $item):
                            $subtotal = $item->preco_unitario_na_compra * $item->quantidade;
                            $total += $subtotal;

                            $nomeImg = !empty($item->imagem) ? $item->imagem : $item->id_produto . '.jpg';
                            $caminhoImg = BASE_URL . "/img/produtos/" . $nomeImg;
                        ?>
                            <tr>
                                <td class="p-3">
                                    <img src="<?= $caminhoImg ?>" alt="Foto"
                                        class="img-thumbnail border-0 bg-light"
                                        style="width: 60px; height: 60px; object-fit: contain;"
                                        onerror="this.src='<?= BASE_URL ?>/img/placeholder.jpg'">
                                </td>
                                <td>
                                    <h6 class="mb-0 text-dark"><?= htmlspecialchars($item->nome) ?></h6>
                                    <small class="text-muted">ID: <?= $item->id_produto ?></small>
                                </td>
                                <td>R$ <?= number_format($item->preco_unitario_na_compra, 2, ',', '.') ?></td>
                                <td><span class="badge bg-light text-dark border"><?= $item->quantidade ?></span></td>
                                <td class="text-end pe-4 fw-bold">R$ <?= number_format($subtotal, 2, ',', '.') ?></td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                    <tfoot>
                        <tr class="bg-light border-top">
                            <td colspan="4" class="text-end fs-5 py-3"><strong>Total do Pedido:</strong></td>
                            <td class="text-end fs-4 fw-bold text-success py-3 pe-4">R$ <?= number_format($total, 2, ',', '.') ?></td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>

<?php include BASE_PATH . '/views/partials/footer.phtml'; ?>