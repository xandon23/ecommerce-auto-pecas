<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $titulo ?? 'Autopeças Dino' ?></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="<?= BASE_URL ?>/css/style.css">
</head>

<body>

    <div class="topbar">
        <div class="container d-flex justify-content-between">
            <div><i class="bi bi-shield-check"></i> Compra 100% Segura</div>
            <div>
                <span class="me-3"><i class="bi bi-whatsapp"></i> (41) 99894-6499</span>
                <a href="<?= BASE_URL ?>/historia">Quem Somos</a>
            </div>
        </div>
    </div>

    <header class="header-main">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-6 col-md-2">
                    <a href="<?= BASE_URL ?>/home">
                        <img src="<?= BASE_URL ?>/img/logodino.png" alt="Dino" style="height: 60px; width: auto;">
                    </a>
                </div>
                <div class="col-12 col-md-6 my-3 my-md-0">
                    <form action="<?= BASE_URL ?>/produto/buscar" method="get" class="d-flex search-container">
                        <input type="search" name="q" class="form-control" placeholder="Busque por peça, marca ou veículo..." required>
                        <button type="submit" class="btn"><i class="bi bi-search"></i></button>
                    </form>
                </div>
                <div class="col-6 col-md-4">
                    <div class="d-flex justify-content-end align-items-center user-actions">
                        <div class="d-flex align-items-center me-4">
                            <i class="bi bi-person-circle"></i>
                            <div>
                                <?php if (isset($_SESSION['usuario'])): ?>
                                    <span>Olá, <?= explode(' ', $_SESSION['usuario']['nome'])[0] ?></span><br>
                                    <?php if (isset($_SESSION['usuario']['tipo']) && $_SESSION['usuario']['tipo'] === 'admin'): ?>
                                        <a href="<?= BASE_URL ?>/admin/dashboard" class="highlight">PAINEL</a>
                                    <?php else: ?>
                                        <a href="<?= BASE_URL ?>/cliente" class="highlight">MINHA CONTA</a>
                                    <?php endif; ?>
                                    <span class="mx-1">|</span> <a href="<?= BASE_URL ?>/index/sair" class="text-danger">Sair</a>
                                <?php else: ?>
                                    <span>Bem-vindo!</span><br>
                                    <a href="<?= BASE_URL ?>/index" class="highlight">ENTRAR</a> ou <a href="<?= BASE_URL ?>/index/cadastro" class="highlight">CADASTRE-SE</a>
                                <?php endif; ?>
                            </div>
                        </div>
                        <a href="<?= BASE_URL ?>/carrinho" class="position-relative text-white">
                            <i class="bi bi-cart3 fs-2 m-0" style="color: #fff;"></i>
                            <?php if (isset($_SESSION['carrinho']) && count($_SESSION['carrinho']) > 0): ?>
                                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger border border-white">
                                    <?= count($_SESSION['carrinho']) ?>
                                </span>
                            <?php endif; ?>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <nav class="nav-cats shadow-sm mb-4">
        <div class="container">
            <ul class="nav overflow-auto flex-nowrap">
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>/produto/listar"><i class="bi bi-list"></i> TODAS</a></li>
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>/produto/categoria/5">Lubrificantes</a></li>
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>/produto/categoria/1">Suspensão</a></li>
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>/produto/categoria/2">Direção</a></li>
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>/produto/categoria/3">Freios</a></li>
                <li class="nav-item"><a class="nav-link" href="<?= BASE_URL ?>/produto/categoria/4">Motor</a></li>
            </ul>
        </div>
    </nav>