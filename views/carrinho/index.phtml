<?php include BASE_PATH . '/views/partials/header.phtml'; ?>

<main class="container my-5">

  <div class="d-flex align-items-center gap-2 mb-4 text-secondary">
    <i class="bi bi-cart-check fs-2" style="color: var(--brand-color);"></i>
    <div>
      <h2 class="mb-0 fw-bold text-dark">Meu Carrinho</h2>
      <small>Confira os itens antes de finalizar sua compra.</small>
    </div>
  </div>

  <?php if (empty($itens)): ?>
    <div class="alert alert-light border shadow-sm p-5 text-center rounded-3">
      <i class="bi bi-cart-x display-1 text-muted mb-3 d-block"></i>
      <h4 class="fw-bold">Seu carrinho está vazio.</h4>
      <p class="text-muted mb-4">Você ainda não adicionou nenhum produto. Explore nossas ofertas e monte seu pedido.</p>
      <a href="<?= BASE_URL ?>/home" class="btn btn-brand px-4">
        <i class="bi bi-arrow-left"></i> Voltar para a loja
      </a>
    </div>
  <?php else: ?>
    <div class="table-responsive shadow-sm bg-white rounded-3 mb-4">
      <table class="table align-middle mb-0 table-hover">
        <thead class="table-light">
          <tr>
            <th style="width: 100px;">Produto</th>
            <th>Descrição</th>
            <th>Preço Unit.</th>
            <th>Qtd</th>
            <th>Subtotal</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <?php
          $total = 0;
          foreach ($itens as $item):
            $subtotal = $item['preco'] * $item['qtd'];
            $total += $subtotal;
          ?>
            <tr>
              <td class="p-3">
                <img src="<?= $item['img'] ?>" alt="<?= htmlspecialchars($item['nome']) ?>"
                  class="img-thumbnail border-0 bg-light"
                  style="width: 80px; height: 80px; object-fit: contain;">
              </td>
              <td>
                <h6 class="mb-1 text-dark fw-bold"><?= htmlspecialchars($item['nome']) ?></h6>
                <span class="badge bg-light text-dark border">ID: <?= $item['id'] ?></span>
              </td>
              <td class="text-muted">R$ <?= number_format($item['preco'], 2, ',', '.') ?></td>
              <td>
                <div class="input-group input-group-sm" style="width: 110px;">
                  <a href="<?= BASE_URL ?>/carrinho/diminuir/<?= $item['id'] ?>" class="btn btn-outline-secondary" type="button">
                    <i class="bi bi-dash"></i>
                  </a>

                  <input type="text" class="form-control text-center bg-white" value="<?= $item['qtd'] ?>" readonly>

                  <a href="<?= BASE_URL ?>/carrinho/aumentar/<?= $item['id'] ?>" class="btn btn-outline-secondary" type="button">
                    <i class="bi bi-plus"></i>
                  </a>
                </div>
              </td>
              <td><strong class="text-danger">R$ <?= number_format($subtotal, 2, ',', '.') ?></strong></td>
              <td class="text-end pe-3">
                <a href="<?= BASE_URL ?>/carrinho/remover/<?= $item['id'] ?>"
                  class="btn btn-sm btn-outline-danger" title="Remover item">
                  <i class="bi bi-trash"></i>
                </a>
              </td>
            </tr>
          <?php endforeach; ?>
        </tbody>
        <tfoot>
          <tr class="bg-light border-top">
            <td colspan="4" class="text-end fs-5 py-3"><strong>Total do Pedido:</strong></td>
            <td colspan="2" class="fs-4 fw-bold py-3 text-dark">R$ <?= number_format($total, 2, ',', '.') ?></td>
          </tr>
        </tfoot>
      </table>
    </div>

    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-3">
      <a href="<?= BASE_URL ?>/home" class="btn btn-outline-secondary px-4">
        <i class="bi bi-arrow-left"></i> Continuar Comprando
      </a>
      <a href="<?= BASE_URL ?>/carrinho/finalizar" class="btn btn-brand btn-lg px-5 shadow-sm">
        Finalizar Compra <i class="bi bi-check-lg ms-2"></i>
      </a>
    </div>
  <?php endif; ?>
</main>

<?php include BASE_PATH . '/views/partials/footer.phtml'; ?>