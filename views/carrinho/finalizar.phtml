<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <title><?= $titulo ?></title>
</head>
<body class="bg-light">
<div class="container my-5">
    <div class="row">
        <div class="col-md-8">
            <h2 class="mb-4">Resumo do Pedido</h2>
            
            <ul class="list-group mb-3 shadow-sm">
                <?php foreach ($itens as $item): ?>
                <li class="list-group-item d-flex justify-content-between lh-sm">
                    <div class="d-flex align-items-center">
                        <img src="<?= $item['img'] ?>" alt="" style="width: 50px; margin-right: 15px;" class="rounded">
                        <div>
                            <h6 class="my-0"><?= $item['nome'] ?></h6>
                            <small class="text-muted">Qtd: <?= $item['qtd'] ?></small>
                        </div>
                    </div>
                    <span class="text-muted">R$ <?= number_format($item['preco'] * $item['qtd'], 2, ',', '.') ?></span>
                </li>
                <?php endforeach; ?>
                
                <li class="list-group-item d-flex justify-content-between bg-light">
                    <span class="fw-bold text-success">Total (BRL)</span>
                    <strong class="text-success">R$ <?= number_format($total, 2, ',', '.') ?></strong>
                </li>
            </ul>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm border-0 bg-light">
                <div class="card-body">
                    <h4 class="mb-3">Dados de Entrega</h4>
                    <p class="mb-1"><strong>Cliente:</strong> <?= $_SESSION['usuario']['nome'] ?></p>
                    <p class="text-muted small mb-4">O endereço será confirmado na próxima etapa.</p>

                    <div class="d-grid">
                        <button class="btn btn-success btn-lg" onclick="alert('Pedido realizado com sucesso! (Simulação)'); location.href='<?= BASE_URL ?>/home'">
                            Confirmar Compra
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>