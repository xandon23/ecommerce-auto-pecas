<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <title><?= $titulo ?></title>
</head>

<body class="bg-light">
    <div class="col-md-4">
            <div class="card shadow-sm border-0 bg-light">
                <div class="card-body">
                    <h4 class="mb-3">Entrega</h4>
                    
                    <form action="<?= BASE_URL ?>/carrinho/confirmar" method="post">
                        
                        <?php if (empty($enderecos)): ?>
                            <div class="alert alert-warning small">
                                Você não tem endereços cadastrados.
                                <a href="<?= BASE_URL ?>/cliente">Cadastrar agora</a>
                            </div>
                        <?php else: ?>
                            <div class="mb-3">
                                <label class="form-label fw-bold">Escolha o endereço:</label>
                                <?php foreach ($enderecos as $k => $end): ?>
                                    <div class="form-check mb-2">
                                        <input class="form-check-input" type="radio" name="id_endereco" id="end<?= $end->id_endereco ?>" value="<?= $end->id_endereco ?>" <?= $k===0 ? 'checked' : '' ?>>
                                        <label class="form-check-label small" for="end<?= $end->id_endereco ?>">
                                            <?= $end->rua ?>, <?= $end->numero ?> - <?= $end->cidade ?>
                                        </label>
                                    </div>
                                <?php endforeach; ?>
                            </div>
                        <?php endif; ?>

                        <hr>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-success btn-lg" <?= empty($enderecos) ? 'disabled' : '' ?>>
                                Confirmar Compra <i class="bi bi-check-lg"></i>
                            </button>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>