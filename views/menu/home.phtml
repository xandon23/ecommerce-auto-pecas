<?php include BASE_PATH . '/views/partials/header.phtml'; ?>

<section class="container my-4">
    <h5 class="mb-3 fw-bold">Ofertas em destaque</h5>

    <div id="carouselProdutos" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            <?php
            $chunks = array_chunk($carrossel, 3);
            foreach ($chunks as $index => $grupo): ?>
                <div class="carousel-item <?= $index === 0 ? 'active' : '' ?>">
                    <div class="row g-3 justify-content-center">
                        <?php foreach ($grupo as $p): ?>
                            <div class="col-6 col-md-4 col-lg-3">
                                <div class="card card-product-mini h-100">
                                    <img src="<?= htmlspecialchars($p['img']) ?>" class="card-img-top thumb-mini" alt="<?= htmlspecialchars($p['nome']) ?>">
                                    <div class="card-body d-flex flex-column">
                                        <h6 class="card-title text-truncate" title="<?= htmlspecialchars($p['nome']) ?>">
                                            <?= htmlspecialchars($p['nome']) ?>
                                        </h6>
                                        <div class="mb-2">
                                            <div class="price-mini">R$ <?= number_format($p['preco'], 2, ',', '.') ?></div>
                                            <div class="text-muted small">em até 10x sem juros</div>
                                        </div>
                                        <a href="<?= BASE_URL ?>/produto/index/<?= $p['slug'] ?>" class="btn btn-sm btn-outline-secondary mt-auto">Ver</a>
                                    </div>
                                </div>
                            </div>
                        <?php endforeach; ?>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselProdutos" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselProdutos" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
        </button>
    </div>
</section>

<section class="container my-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="h5 m-0">Destaques</h2>
        <a href="<?= BASE_URL ?>/produto/listar">Ver todos</a>
    </div>
    <div class="row g-3">
        <?php foreach ($produtos as $p): ?>
            <div class="col-6 col-md-4 col-lg-3">
                <div class="card card-product h-100 shadow-sm border-0">
                    <img class="thumb card-img-top" src="<?= htmlspecialchars($p['img']) ?>" alt="<?= htmlspecialchars($p['nome']) ?>">
                    <div class="card-body d-flex flex-column">
                        <h6 class="card-title text-truncate" title="<?= htmlspecialchars($p['nome']) ?>">
                            <?= htmlspecialchars($p['nome']) ?>
                        </h6>
                        <div class="mb-2">
                            <?php if (!empty($p['preco_old'])): ?>
                                <div class="price-old">R$ <?= number_format($p['preco_old'], 2, ',', '.') ?></div>
                            <?php endif; ?>
                            <div class="price h5">R$ <?= number_format($p['preco'], 2, ',', '.') ?></div>
                            <div class="text-muted small">em até 10x sem juros</div>
                        </div>
                        <div class="mt-auto d-grid gap-2">
                            <a href="<?= BASE_URL ?>/produto/index/<?= $p['slug'] ?>" class="btn btn-outline-secondary btn-sm">
                                <i class="bi bi-eye"></i> Ver
                            </a>
                            <a href="<?= BASE_URL ?>/carrinho/adicionar/<?= $p['slug'] ?>" class="btn btn-brand btn-sm">
                                <i class="bi bi-cart-plus"></i> Adicionar
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</section>

<section class="container my-5">
    <div class="row g-3">
        <div class="col-md-3">
            <div class="trust-box"><i class="bi bi-truck fs-3"></i><p>Entrega para todo o Brasil</p></div>
        </div>
        <div class="col-md-3">
            <div class="trust-box"><i class="bi bi-shield-check fs-3"></i><p>Compra segura</p></div>
        </div>
        <div class="col-md-3">
            <div class="trust-box"><i class="bi bi-arrow-repeat fs-3"></i><p>Troca garantida</p></div>
        </div>
        <div class="col-md-3">
            <div class="trust-box"><i class="bi bi-credit-card fs-3"></i><p>Pix e cartão</p></div>
        </div>
    </div>
</section>

<?php include BASE_PATH . '/views/partials/footer.phtml'; ?>