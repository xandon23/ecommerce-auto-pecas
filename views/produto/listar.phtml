<div class="container my-5">
    <div class="row mb-4">
        <div class="col-12">
            <h2 class="fw-bold">Todos os Produtos</h2>
            <p class="text-muted">Confira a nossa seleção completa de autopeças.</p>
        </div>
    </div>

    <div class="row g-4">
        <?php if (!empty($produtos)): ?>
            <?php foreach ($produtos as $produto): ?>
                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                    <div class="card h-100 shadow-sm border-0">
                        <a href="<?= BASE_URL ?>/produto/index/<?= $produto->id ?>">
                            <img src="<?= $produto->img ?>" class="card-img-top" alt="<?= $produto->nome ?>" style="height: 200px; object-fit: contain; padding: 10px;">
                        </a>
                        
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title text-truncate">
                                <a href="<?= BASE_URL ?>/produto/index/<?= $produto->id ?>" class="text-decoration-none text-dark">
                                    <?= $produto->nome ?>
                                </a>
                            </h5>

                            <div class="mt-auto">
                                <p class="fs-5 fw-bold text-danger mb-2">
                                    R$ <?= number_format($produto->preco, 2, ',', '.') ?>
                                </p>
                                
                                <div class="d-grid gap-2">
                                    <a href="<?= BASE_URL ?>/produto/index/<?= $produto->id ?>" class="btn btn-outline-dark">
                                        Ver Detalhes
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        <?php else: ?>
            <div class="col-12">
                <div class="alert alert-info">
                    Nenhum produto encontrado no momento.
                </div>
            </div>
        <?php endif; ?>
    </div>
</div>