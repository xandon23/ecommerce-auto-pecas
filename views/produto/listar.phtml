<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <title><?= $titulo ?></title>
</head>
<body class="bg-light">
<div class="container my-5">
    <div class="row mb-4">
        <div class="col-12">
            <h2 class="fw-bold">Todos os Produtos</h2>
            <p class="text-muted">Confira a nossa seleção completa de autopeças.</p>
        </div>
    </div>

    <div class="row g-4">
        <?php if (!empty($produtos)): ?>
            <?php foreach ($produtos as $produto): ?>
                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                    <div class="card h-100 shadow-sm border-0">

                        <!-- Imagem leva para a página de detalhes -->
                        <a href="<?= BASE_URL ?>/?url=produto/index/<?= $produto->id ?>">
                            <img src="<?= $produto->img ?>"
                                 class="card-img-top"
                                 alt="<?= htmlspecialchars($produto->nome) ?>"
                                 style="height: 200px; object-fit: contain; padding: 10px;">
                        </a>
                        
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title text-truncate">
                                <a href="<?= BASE_URL ?>/?url=produto/index/<?= $produto->id ?>"
                                   class="text-decoration-none text-dark">
                                    <?= htmlspecialchars($produto->nome) ?>
                                </a>
                            </h5>

                            <div class="mt-auto">
                                <p class="fs-5 fw-bold text-danger mb-2">
                                    R$ <?= number_format($produto->preco, 2, ',', '.') ?>
                                </p>
                                
                                <div class="d-grid gap-2">
                                    <!-- Ver detalhes -->
                                    <a href="<?= BASE_URL ?>/?url=produto/index/<?= $produto->id ?>"
                                       class="btn btn-outline-dark">
                                        Ver Detalhes
                                    </a>

                                    <!-- Adicionar ao carrinho -->
                                    <a href="<?= BASE_URL ?>/?url=carrinho/adicionar/<?= $produto->id ?>"
                                       class="btn btn-dark">
                                        Adicionar ao Carrinho
                                    </a>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
            <?php endforeach; ?>
        <?php else: ?>
            <div class="col-12">
                <div class="alert alert-info">
                    Nenhum produto encontrado no momento.
                </div>
            </div>
        <?php endif; ?>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>