<?php include BASE_PATH . '/views/partials/header.phtml'; ?>

<div class="container my-5">

    <div class="row mb-4">
        <div class="col-12">
            <h2 class="fw-bold"><?= $titulo ?? 'Todos os Produtos' ?></h2>
            <p class="text-muted">Confira a nossa seleção completa de autopeças.</p>
        </div>
    </div>

    <div class="row g-3">
        <?php if (!empty($produtos)): ?>
            <?php foreach ($produtos as $p): ?>
                <div class="col-6 col-md-4 col-lg-3">

                    <div class="card card-product h-100 shadow-sm border-0">

                        <a href="<?= BASE_URL ?>/produto/index/<?= $p->id ?>">
                            <img src="<?= $p->img ?>" class="thumb card-img-top" alt="<?= htmlspecialchars($p->nome) ?>">
                        </a>

                        <div class="card-body d-flex flex-column">
                            <h6 class="card-title text-truncate" title="<?= htmlspecialchars($p->nome) ?>">
                                <a href="<?= BASE_URL ?>/produto/index/<?= $p->id ?>" class="text-decoration-none text-dark">
                                    <?= htmlspecialchars($p->nome) ?>
                                </a>
                            </h6>

                            <div class="mb-2">
                                <div class="price h5">R$ <?= number_format($p->preco, 2, ',', '.') ?></div>
                                <div class="text-muted small">em até 10x sem juros</div>
                            </div>

                            <div class="mt-auto d-grid gap-2">
                                <a href="<?= BASE_URL ?>/produto/index/<?= $p->id ?>" class="btn btn-outline-secondary btn-sm">
                                    <i class="bi bi-eye"></i> Ver
                                </a>
                                <a href="<?= BASE_URL ?>/carrinho/adicionar/<?= $p->id ?>" class="btn btn-brand btn-sm">
                                    <i class="bi bi-cart-plus"></i> Adicionar
                                </a>
                            </div>
                        </div>
                    </div>

                </div>
            <?php endforeach; ?>
        <?php else: ?>
            <div class="col-12">
                <div class="alert alert-info py-5 text-center">
                    <i class="bi bi-search display-1 mb-3 d-block text-muted"></i>
                    <h4>Nenhum produto encontrado.</h4>
                    <p>Tente buscar por outro termo ou navegue pelas categorias.</p>
                    <a href="<?= BASE_URL ?>/home" class="btn btn-outline-primary mt-3">Voltar para a Loja</a>
                </div>
            </div>
        <?php endif; ?>
    </div>
</div>

<?php include BASE_PATH . '/views/partials/footer.phtml'; ?>