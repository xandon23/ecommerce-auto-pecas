<div class="container my-5">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="<?= BASE_URL ?>/home" class="text-decoration-none">Home</a></li>
            <li class="breadcrumb-item"><a href="<?= BASE_URL ?>/produto/listar" class="text-decoration-none">Produtos</a></li>
            <li class="breadcrumb-item active" aria-current="page"><?= $produto->nome ?></li>
        </ol>
    </nav>

    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="border rounded p-3 bg-white text-center shadow-sm">
                <img src="<?= $produto->img ?>" alt="<?= $produto->nome ?>" class="img-fluid" style="max-height: 400px;">
            </div>
        </div>

        <div class="col-md-6">
            <h1 class="fw-bold mb-2"><?= $produto->nome ?></h1>
            
            <p class="text-muted mb-4">Cód. Produto: #<?= $produto->id ?></p>

            <h2 class="text-danger fw-bold mb-3 display-5">
                R$ <?= number_format($produto->preco, 2, ',', '.') ?>
            </h2>
            
            <p class="mb-4 text-muted">
                Em até 10x sem juros no cartão de crédito.
            </p>

            <div class="d-grid gap-2 d-md-block mb-5">
                <a href="<?= BASE_URL ?>/carrinho/adicionar/<?= $produto->id ?>" class="btn btn-danger btn-lg px-5">
                    <i class="bi bi-cart-plus"></i> Adicionar ao Carrinho
                </a>
            </div>

            <div class="card mt-4">
                <div class="card-header bg-light fw-bold">
                    Descrição do Produto
                </div>
                <div class="card-body">
                    <p class="card-text">
                        <?= nl2br($produto->descricao) ?>
                    </p>
                </div>
            </div>
            
            <div class="card mt-3">
                <div class="card-header bg-light fw-bold">
                    Especificações Técnicas
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li><strong>Estoque disponível:</strong> <?= $produto->estoque ?> unidades</li>
                        </ul>
                </div>
            </div>

        </div>
    </div>
</div>