<?php include BASE_PATH . '/views/partials/header.phtml'; ?>

<div class="container my-5">

  <div class="row">
    <div class="col-md-6 mb-4">
      <div class="border rounded p-3 bg-white text-center shadow-sm">
        <img src="<?= $produto->img ?>" alt="<?= htmlspecialchars($produto->nome) ?>" class="img-fluid" style="max-height: 450px; object-fit: contain;">
      </div>
    </div>

    <div class="col-md-6">
      <h1 class="fw-bold mb-2"><?= htmlspecialchars($produto->nome) ?></h1>

      <p class="text-muted mb-4">Cód. Produto: #<?= $produto->id ?></p>

      <div class="mb-4">
        <h2 class="price display-5 mb-0">R$ <?= number_format($produto->preco, 2, ',', '.') ?></h2>
        <p class="text-muted small">
          Em até 10x sem juros no cartão de crédito.
        </p>
      </div>

      <div class="d-grid gap-2 d-md-block mb-5">
        <a href="<?= BASE_URL ?>/carrinho/adicionar/<?= $produto->id ?>" class="btn btn-brand btn-lg px-5 shadow-sm">
          <i class="bi bi-cart-plus-fill"></i> COMPRAR AGORA
        </a>
      </div>

      <div class="card mb-3 border-0 shadow-sm">
        <div class="card-header bg-white fw-bold border-bottom">
          <i class="bi bi-text-left"></i> Descrição do Produto
        </div>
        <div class="card-body">
          <p class="card-text text-secondary">
            <?= nl2br(htmlspecialchars($produto->descricao)) ?>
          </p>
        </div>
      </div>

      <div class="card border-0 shadow-sm">
        <div class="card-body">
          <ul class="list-unstyled mb-0">
            <li><strong>Estoque disponível:</strong>
              <span class="badge <?= $produto->estoque > 0 ? 'bg-success' : 'bg-danger' ?>">
                <?= $produto->estoque ?> unidades
              </span>
            </li>
          </ul>
        </div>
      </div>

    </div>
  </div>
</div>

<?php include BASE_PATH . '/views/partials/footer.phtml'; ?>