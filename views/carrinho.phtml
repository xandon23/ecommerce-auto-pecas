<div class="container my-5">
    <h2 class="mb-4">Meu Carrinho de Compras</h2>

    <?php if (empty($itens)): ?>
        <div class="alert alert-warning">
            Seu carrinho está vazio. <a href="<?= BASE_URL ?>/home" class="alert-link">Voltar para a loja</a>.
        </div>
    <?php else: ?>
        <div class="table-responsive">
            <table class="table align-middle">
                <thead>
                    <tr>
                        <th style="width: 100px;">Produto</th>
                        <th>Descrição</th>
                        <th>Preço Unit.</th>
                        <th>Qtd</th>
                        <th>Subtotal</th>
                        <th>Ação</th>
                    </tr>
                </thead>
                <tbody>
                    <?php 
                    $total = 0;
                    foreach ($itens as $item): 
                        $subtotal = $item['preco'] * $item['qtd'];
                        $total += $subtotal;
                    ?>
                    <tr>
                        <td>
                            <img src="<?= $item['img'] ?>" alt="<?= $item['nome'] ?>" class="img-thumbnail" style="width: 80px; height: 80px; object-fit: cover;">
                        </td>
                        <td>
                            <h5 class="mb-0"><?= $item['nome'] ?></h5>
                        </td>
                        <td>R$ <?= number_format($item['preco'], 2, ',', '.') ?></td>
                        <td><?= $item['qtd'] ?></td>
                        <td><strong>R$ <?= number_format($subtotal, 2, ',', '.') ?></strong></td>
                        <td>
                            <a href="<?= BASE_URL ?>/carrinho/remover/<?= $item['id'] ?>" class="btn btn-sm btn-outline-danger">
                                <i class="bi bi-trash"></i> Remover
                            </a>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
                <tfoot>
                    <tr class="table-light">
                        <td colspan="4" class="text-end fs-4"><strong>Total:</strong></td>
                        <td colspan="2" class="fs-4 text-danger fw-bold">R$ <?= number_format($total, 2, ',', '.') ?></td>
                    </tr>
                </tfoot>
            </table>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="<?= BASE_URL ?>/home" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Continuar Comprando
            </a>
            <a href="#" class="btn btn-success btn-lg">
                Finalizar Compra <i class="bi bi-check-lg"></i>
            </a>
        </div>
    <?php endif; ?>
</div>